function dropElements(arr, func) {
  let arr2=[]
  
  arr2=arr.filter(func)
  let b=0
  let a=arr.indexOf(arr2[0])
  console.log(arr,arr2,a)
  if(a==-1)return []
  while(b<a){
    
  arr.shift()
  b++
  console.log(arr)
  }
  return arr;
}
